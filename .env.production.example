VITE_API_BASE_URL=/api/v1
VITE_GOOGLE_OAUTH_CLIENT_ID=
# For popup OAuth flow, use "postmessage" (recommended).
# Or set to your deployed app origin (e.g. https://app.example.com) and configure it in Google Cloud Console.
VITE_GOOGLE_OAUTH_REDIRECT_URI=postmessage
